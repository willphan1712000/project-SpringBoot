CREATE TABLE order_items (id BIGINT NOT NULL AUTO_INCREMENT , order_id BIGINT NOT NULL , product_id BIGINT NOT NULL , unit_price DECIMAL NOT NULL , quantity INT NOT NULL , total_price DECIMAL(10,2) NOT NULL , PRIMARY KEY (id)) ENGINE = InnoDB;